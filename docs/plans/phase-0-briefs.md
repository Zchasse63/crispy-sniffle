# Phase 0 — Worker Briefs (authored by Fable 5, 2026-07-14)

Execution model: Sonnet workers, one brief each, disjoint file sets, working-tree edits only.
Global rules for every worker (repeated inline in each dispatch):
- Read every file you touch BEFORE editing. Match existing code style, Tailwind idiom, and type usage exactly.
- ONLY touch the files listed in your brief. If the task seems to require another file, stop and report instead.
- No new dependencies. No migrations. No commits. No `npm run build`, no dev servers (a centralized gate runs them once).
- lucide-react for icons; never raw emoji. Copy tone: Scout voice (plain, honest, no hype).
- Never-fabricate: unknown data renders "unlisted"/nothing — never a made-up value.
- When done, report: files changed, what you did, anything you noticed but did NOT fix (out of scope), and any type-safety concerns.

## W1 — Route loading states
Files: `src/app/loading.tsx` (new), `src/app/gym/[slug]/loading.tsx` (new), `src/app/me/loading.tsx` (new), `src/app/trips/loading.tsx` (new), `src/app/compare/loading.tsx` (new). Read-only: `src/components/ui/Skeleton.tsx`, the corresponding `page.tsx` files, `src/app/layout.tsx`.
Problem: /, /gym/[slug], /me, /trips, /compare are force-dynamic RSCs with multi-query Supabase fetches and there is no loading.tsx anywhere — every navigation freezes the previous screen. `GymCardSkeleton`/`GymListSkeleton` exist in Skeleton.tsx and are imported by nothing.
Do: create the five loading.tsx files reusing the existing skeleton components wherever they fit; where they don't (detail hero, compare table, trips list), compose simple pulse blocks in the same visual style (bg tone + rounded + animate-pulse as used in Skeleton.tsx). Approximate each page's real layout (e.g. detail = dark hero band + action-row pills + content blocks; discovery = hero band + card grid via GymListSkeleton). Respect prefers-reduced-motion if Skeleton.tsx does; if it doesn't, don't add a new system.

## W2 — Completeness-first default sort
Files: `src/lib/completeness.ts` (new), `src/lib/admin/gyms-admin.ts`, `src/lib/scoring/scorer.ts`, `src/lib/scoring/scorer.test.ts`.
Problem: browse order is effectively alphabetical — `byRatingThenName` tiebreak is dead code in prod (rating is NULL for all 836 gyms), so the front door opens on "10th Planet Jiu Jitsu St. Pete" and buries the 35 rich-tier gyms.
Do: (1) Extract the completeness metric from `src/lib/admin/gyms-admin.ts:8-28` (CORE_FIELDS + completeness()) into new shared `src/lib/completeness.ts` typed against EnrichedGym; make the admin lib import it (one implementation per concern — repo rule 5). (2) In `scorer.ts` (sort sites at ~62-71 and ~267-279): replace the ordering with matchScore desc → completeness desc → rating desc nulls-last → name asc, in BOTH the empty-filter branch and the scored tiebreak. (3) Update `scorer.test.ts` (current assertions at ~11-14 assert rating-then-name) and add cases: completeness beats name order; null-rating gyms don't sink below lower-completeness gyms; scored order still leads with matchScore. Run `npx vitest run src/lib/scoring/scorer.test.ts` and report output. Do NOT touch DiscoveryClient, search_logs, or the weak-match banner (they must keep reading match order).

## W3 — Owner portal trust triage
Files: `src/components/owner/OwnerFormShell.tsx`, `src/components/owner/ReviewScreen.tsx`, `src/app/own/[token]/page.tsx`, `src/lib/owner/formConfig.ts` (+ `src/lib/owner/*` only if the payload shape requires it — read first).
Problem (4 confirmed trust breaks): (a) success screen says "In the live version this goes to a quick human review" — real owners conclude they filled out a demo; (b) expired/used invite tokens fall through to the site 404 ("No waypoint here") implying the gym closed; (c) the "I'm done for now" short path submits with zero contact info so Scout can't verify or follow up; (d) post-submit "Review my answers" silently discards corrections (submitOnce guard + invite already flipped to used).
Do: (a) Rewrite the completion screen copy (~OwnerFormShell.tsx:571-584): thank them, state concretely what happens next ("Scout reviews submissions within 2 business days; your listing updates as soon as facts are approved"), and show the contact-email confirmation when one was provided. (b) In `own/[token]/page.tsx` (~line 41): render a dedicated on-brand expired/used-invite screen instead of notFound() — explain the link expired, that their gym is still on Scout, and give a "request a fresh link" CTA (mailto to the address already used elsewhere in the owner flow, or /contact if no such address exists — check first). (c) Add an OPTIONAL email field to the short-path finish step in formConfig/OwnerFormShell so "I'm done for now" can carry contact info; wire it into the existing submission payload ONLY if the payload is a flexible shape (jsonb-style answers object) — read the submit path first; if it would require a DB migration, implement (a)(b)(d), skip (c), and report the blocker precisely. (d) Post-submit: make "Review my answers" honest — show answers read-only with a banner: "Submitted — need a correction? Email us and we'll fix it" (link per (b)); do not fake editability.

## W4 — Auth round-trip + sign-in modal pitch
Files: `src/components/auth/SignInModal.tsx`, `src/components/gym/TrainHereButton.tsx`, `src/components/auth/AuthGate.tsx`, `src/components/profile/ProfilePortal.tsx`, `src/lib/pendingAction.ts` (new). Read-only: `src/app/auth/callback/route.ts` (its same-origin ?next guard already exists — use, don't modify).
Problem: magic links strand users on /me (emailRedirectTo carries no next param); sign-in triggered by "I trained here" never logs the visit afterward; signing out on /me leaves the stale SSR profile rendered; the modal never says WHY to sign in.
Do: (1) SignInModal (~lines 49-55): include `?next=<current pathname+search>` in emailRedirectTo so the existing callback guard returns the user where they were; apply the same to the password flow's post-success behavior if it currently redirects. (2) New `src/lib/pendingAction.ts`: tiny localStorage helper — save/read/clear `{ type: 'train_here', gymId: string, ts: number }`, 30-minute TTL, JSON-safe, no store framework. (3) TrainHereButton (~lines 17-21): when the click hits the auth gate, persist the pending action before opening the sign-in modal; on mount-when-authenticated (or via the AuthGate hook below) if a fresh pending action matches this gym, complete the visit log exactly as the normal signed-in path does, clear it, and show the existing success state. (4) AuthGate: on SIGNED_IN, read pendingAction; if fresh, let the destination page's TrainHereButton consume it (keep the mechanism simple — the button checks on mount; AuthGate only needs to avoid clearing it). (5) ProfilePortal (~line 31): after signOut(), call `router.refresh()` (and route to `/` if the current page is signed-in-only) so no stale profile remains. (6) Add a compact value-pitch block to the modal above the method tabs: three short benefits (sync saves & trips across devices · log visits and get honest membership-vs-day-pass math · follow gyms for change alerts). Keep the existing "One email, one tap" microcopy.

## W5 — Match context on gym detail + lazy card images
Files: `src/components/gym/GymCard.tsx`, `src/components/gym/MatchContext.tsx` (new), `src/app/gym/[slug]/page.tsx`. Read-only: `src/lib/scoring/scorer.ts`, `src/lib/types/scout.ts`, `src/stores/filterStore.ts`, `src/components/gym/MatchBadge.tsx`.
Problem: (a) GymCard's `<img>` (~lines 61-66) loads eagerly — 538 images on the front door; (b) the match score/reasons that sold the click are explicitly discarded on the detail page (`page.tsx` ~line 191 nulls matchScore), killing "why Scout picked this" exactly where the decision happens.
Do: (a) Add `loading="lazy"` + `decoding="async"` to GymCard's img. (b) New client component `MatchContext.tsx`: reads the CURRENT FilterSet from filterStore (session-only — this is fine and intended); if `isEmptyFilterSet` (scout.ts ~108-123) render nothing; otherwise run the deterministic scorer for THIS gym (`scoreGyms([gym], filters, new Date())`) and render a compact panel: MatchBadge with the score + "Why it fits" reasons + first missing item, reusing the exact visual language GymCard uses for its reasons row (read GymCard first and mirror it; reuse MatchBadge, don't fork). (c) Mount `<MatchContext gym={...} />` on the detail page directly below the hero action row, passing the already-fetched EnrichedGym; remove/bypass the matchScore-nulling at ~line 191 in favor of this island. Scoring must come from the shared scorer only — no local scoring logic. Direct visits (no active search) correctly render nothing.

## W6 — global-error, /contact page, mailto sweep
Files: `src/app/global-error.tsx` (new), `src/app/contact/page.tsx` (new), `src/lib/contactInfo.ts` (new), `src/components/SiteFooter.tsx`, `src/app/about/page.tsx`, `src/app/privacy/page.tsx`, `src/app/terms/page.tsx`. Read-only: the existing 404 page (find it — "No waypoint here"), `src/app/layout.tsx`.
Problem: root-layout crashes render Next's unbranded default; there is no /contact route; every contact affordance across footer/about/privacy/terms is a personal-Gmail mailto.
Do: (1) `global-error.tsx` in the 404's visual voice ("Signal lost" style heading, brief honest copy, "Back to Explore" button + "Try again" via the reset() prop) — remember global-error must render its own `<html><body>` per Next.js and can't rely on the root layout. (2) `src/lib/contactInfo.ts`: export the single canonical contact email constant (use the address currently in the footer mailtos) + helper `mailtoHref(subject?)`. (3) `/contact` page, on-brand and simple: what to contact us about (data corrections, gym owners, feedback, press), the email as the single CTA (from contactInfo), link to the owner flow note ("Run a gym? Mention it — we'll send a verification link"), and a link back to Explore. (4) Sweep SiteFooter/about/privacy/terms: replace raw personal mailtos with either a link to /contact (preferred for "contact us" prose) or `mailtoHref()` where an email is contextually right (e.g. footer "Send feedback" can keep a mailto but through the constant). No other copy changes.

## Verification gate (Fable 5, after all workers)
1. `npx tsc --noEmit` + `npm run build` + `npx vitest run` (detached-nohup per repo workflow).
2. Full-diff review (correctness, conventions, do-not-break list).
3. Dev-server walkthrough: loading skeletons, contact page, expired-token screen, sign-in modal pitch, match panel after a search, sign-out refresh.
4. Fix or re-dispatch anything that fails. Report results honestly.
